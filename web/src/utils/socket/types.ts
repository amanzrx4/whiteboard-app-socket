export enum SocketEventClient {
  ELEMENTS_STORE_STATE = "elements_store_state",
}

export enum SocketEventClientCommon {
  CONNECT = "connect",
  DISCONNECT = "disconnect",
  RECONNECT = "reconnect",
}
